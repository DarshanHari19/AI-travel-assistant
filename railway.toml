[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd backend-agent && python -m uvicorn agent:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
