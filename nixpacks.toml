[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = [
  "python -m pip install -r backend-agent/requirements.txt"
]

[start]
cmd = "cd backend-agent && python -m uvicorn agent:app --host 0.0.0.0 --port $PORT"
